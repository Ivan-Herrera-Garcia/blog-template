/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    domains: ['encrypted-tbn0.gstatic.com', 'static.vecteezy.com', 'static1.cbrimages.com', 'res.cloudinary.com'],
  },
};

export default nextConfig;
